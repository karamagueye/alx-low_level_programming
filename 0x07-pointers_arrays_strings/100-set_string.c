#include "main.h"
#include <stdio.h>
/**
 * set_string - Sets the value of a pointer to a char.
 * @s: Pointer to a pointer to a char (destination).
 * @to: Pointer to a char (source).
 */
void set_string(char **s, char *to)
{
	*so = to;
}


