#include "main.h"
#include <stdio.h>
/**
 * Description:  function that sets the value of a pointer to a char,
 * the name is so and the result is to.
 */
void set_string(char **s, char *to);

{
	*so = to;
}

